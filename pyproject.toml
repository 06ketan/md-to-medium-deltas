[project]
name = "md-to-medium-deltas"
version = "0.1.0"
description = "Convert Markdown to Medium's internal JSON delta protocol for programmatic publishing."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "markdown>=3.7",
    "mdx-linkify>=2.1",
    "selectolax>=0.4.9",
]
